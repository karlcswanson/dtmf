{
  "name": "dtmf",
  "version": "1.0.0",
  "description": "Bridge Twilio to Route53 with Lambda",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "pack": "zip -r function.zip ./",
    "push": "aws lambda update-function-code --function-name helloWorld --zip-file fileb://function.zip"
  },
  "author": "karlcswamson@gmail.com",
  "license": "MIT",
  "dependencies": {
    "aws-sdk": "^2.751.0",
    "twilio": "^3.49.1"
  },
  "devDependencies": {
    "eslint": "^7.9.0",
    "eslint-config-airbnb-base": "^14.2.0",
    "eslint-plugin-import": "^2.22.0"
  }
}
